# Copy to .env for local development. NEVER commit the real .env.

# --- Cloudflare (Bulk Redirects — set your own account id + zone id below) ---
CF_API_TOKEN=changeme
CF_ACCOUNT_ID=changeme
CF_ZONE_ID_JODACREATIVESTUDIO=changeme

# --- Server ---
CUEARCODE_HOST=0.0.0.0
CUEARCODE_PORT=7900
CUEARCODE_DB_PATH=./data/cuearcode.db
CUEARCODE_LOG_LEVEL=info
