// Mirrors src/constants.ts on the backend (see that file's comment for why
// this is duplicated rather than shared).
export const REDIRECT_HOST = 'go.jodacreativestudio.com';
export const REDIRECT_BASE_URL = `https://${REDIRECT_HOST}/r`;
